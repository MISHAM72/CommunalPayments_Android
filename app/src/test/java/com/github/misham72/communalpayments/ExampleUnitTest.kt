package com.github.misham72.communalpayments

import org.assertj.core.api.Assertions.assertThat
import org.junit.Test

class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertThat(2 + 500).isEqualTo(502)
    }
}